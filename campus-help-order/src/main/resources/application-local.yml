server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:campus_help;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: root
    password: 233615
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema-order.sql

  h2:
    console:
      enabled: true
      path: /h2-console

campus:
  order:
    pay-timeout-minutes: 15
    expire-scan-ms: 30000

