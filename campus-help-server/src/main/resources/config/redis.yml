# -----------------------------------------------------------------------------
spring:
  redis:
    host: ${REDIS_HOST:127.0.0.1}
    port: ${REDIS_PORT:6379}
    # 开发环境 Redis 无密码时注释掉此行，或设置 REDIS_PASSWORD 环境变量
    # password: ${REDIS_PASSWORD:}
    database: ${REDIS_DB:0}
